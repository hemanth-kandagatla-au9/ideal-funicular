import PlanningRoute from "../routes/PlanningRoute";

 /**
  * Testing PlanningRoute.
  */
  test('PlanningRoute', () => {
    expect(PlanningRoute).not.toBeUndefined();
    expect(PlanningRoute).not.toBeNull();
    expect(PlanningRoute).not.toBe(null);
  });
  