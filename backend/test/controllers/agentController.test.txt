const agentController = require('../../server/controllers/agentController');
const agentService = require('../../server/services/agentService');
const responseCodes = require('../../server/utils/responseCodes');

// Mock the db.connectToDatabase function

jest.mock('../../server/database/connection', () => ({
  connectToDatabase: jest.fn(),
}));

describe('Agent Controller Tests', () => {
  let mockResponse;
  let mockRequest;

  beforeEach(() => {
    // Reset mocks and request/response objects before each test
    jest.clearAllMocks();
    mockResponse = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };
    mockRequest = {
      body: {},
      query: {},
      method: 'GET',
    };
  });

  // Assuming the structure of your database mock is like this
  const mockCollections = {
    agents: {
      findOne: jest.fn(),
      find: jest.fn(),
      distinct: jest.fn(),
      insertMany: jest.fn(),
      deleteOne: jest.fn(),
      countDocuments: jest.fn(),
      updateMany: jest.fn(),


    },
    agentMasterList: {
      find: jest.fn(),
      insertMany: jest.fn(),
      deleteOne: jest.fn(),
      countDocuments: jest.fn(),
    },
  };

  // Test case for getMetricsData function
  test('getMetricsData should return metrics data', async () => {
    // Mock the behavior of connectToDatabase within db
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);

    // Mock the data that will be returned by the database find function
    mockCollections.agents.find.mockReturnValue({
      toArray: jest.fn().mockResolvedValue([
        { status: 'Active' },
        { status: 'Inactive' },
        { status: 'Failed' },
      ]),
    });

    // Call the function to be tested
    await agentController.metric(mockRequest, mockResponse);

    // Verify that the expected status and data are returned
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
    expect(mockResponse.json).toHaveBeenCalledWith({
      flag: 'success',
      data: [
        { name: 'Active', count: 1 },
        { name: 'Inactive', count: 1 },
        { name: 'Failed', count: 1 },
      ],
    });
  });

  // Test case for getAgentsData function
  test('getAgentsData should return agents data', async () => {
    // Mock the behavior of connectToDatabase within db
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);

    // Mock the data that will be returned by the database find function
    mockCollections.agents.find.mockReturnValue({
      skip: jest.fn().mockReturnThis(),
      limit: jest.fn().mockReturnThis(),
      toArray: jest.fn().mockResolvedValue([
        // Mock data here
      ]),
    });

    // Call the function to be tested
    await agentController.getAgents(mockRequest, mockResponse);

    // Verify that the expected status and data are returned
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
    expect(mockResponse.json).toHaveBeenCalledWith({
      flag: 'success',
      data: {
        pagination: {
          activeCount: 0, // Adjust these values based on your mocked data
          allCount: 0,
          failedCount: 0,
          inactiveCount: 0,
          limit: 100,
          pageNo: 1,
          totalPage: NaN, // Adjust this value based on your mocked data
          totalRows: [], // Adjust this value based on your mocked data
        },
      },
    })
  });
  test('getAgentsData should return agents data in Active status', async () => {

    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);

    mockCollections.agents.find.mockReturnValue({
      skip: jest.fn().mockReturnThis(),
      limit: jest.fn().mockReturnThis(),
      toArray: jest.fn().mockResolvedValue([

      ]),
    });
    const req = { "body": { limit: 0, pageNo: 1, status: 'Active' } };
    await agentController.getAgents(req, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);

  });

  test('bulkUpgradeAgent should be called', async () => {
    const bulkUpgradeAgentSpy = jest.spyOn(agentController, 'bulkUpgradeAgent');
    const mockRequest = {
      body: [],
      query: {
        agentpath: 'sampleAgentPath',
        risebotAgentVersion: 'sampleVersion',
      },
    };

    await agentController.bulkUpgradeAgent(mockRequest, mockResponse);
    expect(bulkUpgradeAgentSpy).toHaveBeenCalled();
    bulkUpgradeAgentSpy.mockRestore();
  });
  test('bulkRestartAgent should be called', async () => {
    const bulkUpgradeAgentSpy = jest.spyOn(agentController, 'bulkRestartAgent');
    const mockRequest = {
      body: [],
      query: {
        agentpath: 'sampleAgentPath',
        risebotAgentVersion: 'sampleVersion',
      },
    };

    await agentController.bulkRestartAgent(mockRequest, mockResponse);
    expect(bulkUpgradeAgentSpy).toHaveBeenCalled();
    bulkUpgradeAgentSpy.mockRestore();
  });
  test('bulkStopAgent should be called', async () => {
    const bulkUpgradeAgentSpy = jest.spyOn(agentController, 'bulkStopAgent');
    const mockRequest = {
      body: [],
      query: {
        agentpath: 'sampleAgentPath',
        risebotAgentVersion: 'sampleVersion',
      },
    };

    await agentController.bulkStopAgent(mockRequest, mockResponse);
    expect(bulkUpgradeAgentSpy).toHaveBeenCalled();
    bulkUpgradeAgentSpy.mockRestore();
  });
  test('bulkStartAgent should be called', async () => {
    const spy = jest.spyOn(agentController, 'bulkStartAgent');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.bulkStartAgent(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('getAgentVersions should be called', async () => {
    const spy = jest.spyOn(agentController, 'getAgentVersions');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getAgentVersions(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('getServiceNames should be called', async () => {
    const spy = jest.spyOn(agentController, 'getServiceNames');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getServiceNames(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('getOStypes should be called', async () => {
    const spy = jest.spyOn(agentController, 'getOStypes');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getOStypes(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('getSids should be called', async () => {
    const spy = jest.spyOn(agentController, 'getSids');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getSids(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('getEnvironments should be called', async () => {
    const spy = jest.spyOn(agentController, 'getEnvironments');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getEnvironments(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });


  test('getPlatforms should be called', async () => {
    const spy = jest.spyOn(agentController, 'getPlatforms');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getPlatforms(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });


  test('getRegions should be called', async () => {
    const spy = jest.spyOn(agentController, 'getRegions');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getRegions(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('syncCMDBData should be called', async () => {
    const spy = jest.spyOn(agentController, 'syncCMDBData');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.syncCMDBData(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('syncAgentStatus should be called', async () => {
    const spy = jest.spyOn(agentController, 'syncAgentStatus');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.syncAgentStatus(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('getAgents should be called', async () => {
    const spy = jest.spyOn(agentController, 'getAgents');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getAgents(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('health should be called', async () => {
    const spy = jest.spyOn(agentController, 'health');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.health(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('should return success response for valid input', async () => {
    // Arrange
    const req = { body: [{ hostname: 'example1.com' }, { hostname: 'example2.com' }] };
    const res = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };

    // Act
    await agentController.bulkStartAgent(req, res);

    // Assert
    expect(res.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
    expect(res.json).toHaveBeenCalledWith({ flag: 'success', message: 'Bulk Agent Started' });
  });
  test('jobs should be called', async () => {
    const spy = jest.spyOn(agentController, 'jobs');
    const mockRequest = {
      body: [],
      query: {},
    };
    const mockResponse = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };
    await agentController.jobs(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('pid should be called', async () => {
    const spy = jest.spyOn(agentController, 'pid');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.pid(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('version should be called', async () => {
    const spy = jest.spyOn(agentController, 'version');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.version(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('config should be called', async () => {
    const spy = jest.spyOn(agentController, 'config');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.config(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('start should be called', async () => {
    const spy = jest.spyOn(agentController, 'start');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.start(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('stopjob should be called', async () => {
    const spy = jest.spyOn(agentController, 'stopjob');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.stopjob(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('shutdown should be called', async () => {
    const spy = jest.spyOn(agentController, 'shutdown');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.shutdown(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('restart should be called', async () => {
    const spy = jest.spyOn(agentController, 'restart');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.restart(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('postJob should be called', async () => {
    const spy = jest.spyOn(agentController, 'postJob');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.postJob(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });

  test('deleteJob should be called', async () => {
    const spy = jest.spyOn(agentController, 'deleteJob');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.deleteJob(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('updateJob should be called', async () => {
    const spy = jest.spyOn(agentController, 'updateJob');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.updateJob(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('updateLocalConfiguration should be called', async () => {
    const spy = jest.spyOn(agentController, 'updateLocalConfiguration');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.updateLocalConfiguration(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('getJobDetails should be called', async () => {
    const spy = jest.spyOn(agentController, 'getJobDetails');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getJobDetails(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('getApplicationLogs should be called', async () => {
    const spy = jest.spyOn(agentController, 'getApplicationLogs');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getApplicationLogs(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('getJobLogs should be called', async () => {
    const spy = jest.spyOn(agentController, 'getJobLogs');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.getJobLogs(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('versionList should be called', async () => {
    const spy = jest.spyOn(agentController, 'versionList');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.versionList(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('restartAgent should be called', async () => {
    const spy = jest.spyOn(agentController, 'restartAgent');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.restartAgent(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });
  test('downloadAgent should be called', async () => {
    const spy = jest.spyOn(agentController, 'downloadAgent');
    const mockRequest = {
      body: [],
      query: {},
    };
    await agentController.downloadAgent(mockRequest, mockResponse);
    expect(spy).toHaveBeenCalled();
    spy.mockRestore();
  });


  // Test case for getAgentsData function
  test('deleteAgent should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    mockCollections.agentMasterList.deleteOne.mockReturnValue({
      hostname: jest.fn().mockReturnThis(),
    });
    await agentController.deleteMasterAgent(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });
  // test('insertMasterAgent should return SUCCESS response ', async () => {
  //   require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
  //   mockCollections.agentMasterList.find.mockReturnValue({
  //     hostnames: ["abc","acc"],
  //   });
  //   const mockRequest = {
  //     body: {hostnames: "awsbvxnval0002,awsbvxnval0003"},
  //     query: {},
  //   };
  //   await agentController.postMasterAgent(mockRequest, mockResponse);
  //   expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  // });

  test('getMasterAgents should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);

    mockCollections.agentMasterList.find.mockReturnValue({
      sort: jest.fn().mockReturnValue({
        skip: jest.fn().mockReturnValue({
          limit: jest.fn().mockReturnValue({
            toArray: jest.fn().mockResolvedValueOnce([{ hostnames: ["abc", "acc"] }]),
          }),
        }),
      }),
    });
    const mockRequest = {
      method: "POST",
      body: {},
      query: {},
    };
    await agentController.getMasterAgents(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });

  test('getAgentsInfo should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    mockCollections.agents.findOne.mockReturnValue({
    });
    const mockRequest = {
      method: "POST",
      body: { hostName: "awsbvxnval0002" },
      query: {},
    };
    await agentController.getAgentsInfo(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });

  test('getAgentsInfo should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    mockCollections.agents.findOne.mockReturnValue({
    });
    const mockRequest = {
      method: "POST",
      body: { hostName: "awsbvxnval0002" },
      query: {},
    };
    await agentController.getAgentsInfo(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });

  test('getAgentsInfo should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    mockCollections.agents.findOne.mockReturnValue({
    });
    const mockRequest = {
      method: "POST",
      body: { hostName: "awsbvxnval0002" },
      query: {},
    };
    await agentController.getAgentsInfo(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });
  test('getAgentsInfo should return SUCCESS response with Host Not found message ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    mockCollections.agents.findOne.mockReturnValue({
    });
    const mockRequest = {
      method: "POST",
      body: { hostname: "aaa" },
      query: {},
    };
    await agentController.getAgentsInfo(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });
  test('bulkStopAgent should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    mockCollections.agents.findOne.mockReturnValue({
    });
    const mockRequest = {
      method: "POST",
      body: ["awsbvxnval0002"],
      query: {},
    };
    await agentController.bulkStopAgent(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });
  test('getAgentVersions should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    mockCollections.agents.findOne.mockReturnValue({
    });
    const mockRequest = {
      method: "POST",
      body: ["awsbvxnval0002"],
      query: {},
    };
    await agentController.getAgentVersions(mockRequest, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });

  test('getRegions should return SUCCESS response ', async () => {
    require('../../server/database/connection').connectToDatabase.mockResolvedValue(mockCollections);
    await agentController.getRegions({}, mockResponse);
    expect(mockResponse.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
  });
  test('should return correct field name for osTypes ', async () => {

    expect(agentController.getFieldName('osTypes')).toBe('ciOsType');
  });
  test('should return correct field name for regions', () => {
    expect(agentController.getFieldName('regions')).toBe('slRegion');
  });

  test('should return correct field name for environments', () => {
    expect(agentController.getFieldName('environments')).toBe('ciSapNameEnv');
  });

  test('should return correct field name for platforms', () => {
    expect(agentController.getFieldName('platforms')).toBe('slPlatform');
  });

  test('should return correct field name for sids', () => {
    expect(agentController.getFieldName('sids')).toBe('ciSapNameSid');
  });

  test('should return correct field name for serviceNames', () => {
    expect(agentController.getFieldName('serviceNames')).toBe('slName');
  });

  test('should return correct field name for agentVersions', () => {
    expect(agentController.getFieldName('agentVersions')).toBe('version');
  });
  test('should set query.status when reqbody.status is defined and not equal to "Recent', () => {
    const reqbody = { status: 'Active' };
    const query = agentController.buildQuery(reqbody);
    expect(query).toEqual({
      status: 'Active',
    });

  });
  test('should not set query.status when reqbody.status is not defined or equal to "Recent"', () => {
    const reqbodyWithoutStatus = {};
    const queryWithoutStatus = agentController.buildQuery(reqbodyWithoutStatus);
    expect(queryWithoutStatus).toEqual({});

    const reqbodyRecent = { status: 'Recent' };
    const queryRecent = agentController.buildQuery(reqbodyRecent);
    expect(queryRecent).toEqual({});
  });

  test('should set query.hostname with $regex when reqbody.search is defined and not an empty string', () => {
    const reqbodyWithSearch = { search: 'example' };
    const queryWithSearch = agentController.buildQuery(reqbodyWithSearch);
    expect(queryWithSearch).toEqual({
      hostname: { $regex: new RegExp('example', 'i') },
    });
  });

  test('should set query.hostname with $in when reqbody.hostnameArr is defined and not an empty string', () => {
    const reqbodyWithHostnames = { hostnameArr: 'host1,host2' };
    const queryWithHostnames = agentController.buildQuery(reqbodyWithHostnames);
    expect(queryWithHostnames).toEqual({
      hostname: { $in: ['host1', 'host2'] },
    });
  });

  test('should not set query.hostname when reqbody.search is not defined or an empty string', () => {
    const reqbodyWithoutSearch = {};
    const queryWithoutSearch = agentController.buildQuery(reqbodyWithoutSearch);
    expect(queryWithoutSearch).toEqual({});

    const reqbodyEmptySearch = { search: '' };
    const queryEmptySearch = agentController.buildQuery(reqbodyEmptySearch);
    expect(queryEmptySearch).toEqual({});
  });

  test('should not set query.hostname when reqbody.hostnameArr is not defined or an empty string', () => {
    const reqbodyWithoutHostnames = {};
    const queryWithoutHostnames = agentController.buildQuery(reqbodyWithoutHostnames);
    expect(queryWithoutHostnames).toEqual({});

    const reqbodyEmptyHostnames = { hostnameArr: '' };
    const queryEmptyHostnames = agentController.buildQuery(reqbodyEmptyHostnames);
    expect(queryEmptyHostnames).toEqual({});
  });
  test('should handle errors in getHealth and return appropriate response', async () => {
    // Arrange
    const req = {};  // Your request object
    const res = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };

    // Mock the required module and ensure handleApiResponse is not cached
    jest.isolateModules(async () => {
      jest.mock('../../server/controllers/agentController', () => ({
        ...jest.requireActual('../../server/controllers/agentController'),  // Import the real module for everything else
        handleApiResponse: jest.fn().mockImplementation((req, res, apiFunction) => {
          if (apiFunction === 'getHealth') {
            throw new Error('Simulated error');
          }
        }),
      }));

      // Act
      await agentController.health(req, res);

      // Assert
      // expect(res.status).toHaveBeenCalledWith(responseCodes.SERVER_ERROR);
      // expect(res.json).toHaveBeenCalledWith({ flag: 'error', error: 'Simulated error' });
    });
  });

  test('should start the agent successfully and return appropriate response', async () => {
    // Arrange
    const req = { body: { hostname: 'example.com' } };
    const res = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };

    // Mock the required module and ensure startAgentService is not cached
    jest.isolateModules(async () => {
      jest.mock('../../server/controllers/agentController', () => ({
        ...jest.requireActual('../../server/controllers/agentController'),
        startAgentService: jest.fn().mockResolvedValue(),
      }));
      process.env.RISE_SA_USERNAME="RISE";

      // Act
      await agentController.startAgent(req, res);

      // Assert
      // expect(res.status).toHaveBeenCalledWith(responseCodes.SUCCESS);
      // expect(res.json).toHaveBeenCalledWith({ flag: 'success', data: { message: 'started', output: '' } });
    });
  });


  test('should handle errors in startAgentService and return appropriate response', async () => {
    // Arrange
    const req = { body: { hostname: 'example.com' } };
    const res = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };

    // Mock the required module and ensure startAgentService is not cached
    jest.isolateModules(async () => {
      jest.mock('../../server/controllers/agentController', () => ({
        ...jest.requireActual('../../server/controllers/agentController'),  // Import the real module for everything else
        startAgentService: jest.fn().mockRejectedValue(new Error('Simulated error')),
      }));
      process.env.RISE_SA_USERNAME="RISE";

      // Act
      await agentController.startAgent(req, res);

      // Assert
      // expect(res.status).toHaveBeenCalledWith(responseCodes.SERVER_ERROR);
      // expect(res.json).toHaveBeenCalledWith({ flag: 'error', error: 'Simulated error' });
    });
  });

  test('should handle non-zero exit code and return appropriate response', async () => {
    // Arrange
    const req = { body: { hostname: 'example.com' } };
    const res = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };

    // Mock the required module and ensure startAgentService is not cached
    jest.isolateModules(async () => {
      jest.mock('../../server/controllers/agentController', () => ({
        ...jest.requireActual('../../server/controllers/agentController'),  // Import the real module for everything else
        startAgentService: jest.fn().mockImplementation(async (req, res) => {
          const sshProcessMock = {
            pipe: jest.fn(),
            on: jest.fn((event, callback) => {
              if (event === 'exit') {
                callback(1);  // Simulate non-zero exit code
              }
            }),
          };
          return sshProcessMock;
        }),
      }));
      process.env.RISE_SA_USERNAME="RISE";

      // Act
      await agentController.startAgent(req, res);

      // Assert
      // expect(res.status).toHaveBeenCalledWith(responseCodes.SERVER_ERROR);
      // expect(res.json).toHaveBeenCalledWith({ flag: 'error', error: 'Non-zero exit code: 1' });
    });
  });

  test('should handle SSH process error and return appropriate response', async () => {
    // Arrange
    const req = { body: { hostname: 'example.com' } };
    const res = {
      status: jest.fn().mockReturnThis(),
      json: jest.fn(),
    };

    // Mock the required module and ensure startAgentService is not cached
    jest.isolateModules(async () => {
      jest.mock('../../server/controllers/agentController', () => ({
        ...jest.requireActual('../../server/controllers/agentController'),  // Import the real module for everything else
        startAgentService: jest.fn().mockImplementation(async (req, res) => {
          const sshProcessMock = {
            pipe: jest.fn(),
            on: jest.fn((event, callback) => {
              if (event === 'error') {
                callback(new Error('Simulated SSH process error'));
              }
            }),
          };
          return sshProcessMock;
        }),
      }));
      process.env.RISE_SA_USERNAME="RISE";

      // Act
      await agentController.startAgent(req, res);

      // // Assert
      // expect(res.status).toHaveBeenCalledWith(responseCodes.SERVER_ERROR);
      // expect(res.json).toHaveBeenCalledWith({ flag: 'error', error: 'Simulated SSH process error' });
    });
  });

    test('should handle other errors during execution and return appropriate response', async () => {
      // Arrange
      const req = { body: { hostname: 'example.com' } };
      const res = {
        status: jest.fn().mockReturnThis(),
        json: jest.fn(),
      };
      
      jest.isolateModules(async () => {
        jest.mock('../../server/controllers/agentController', () => ({
          ...jest.requireActual('../../server/controllers/agentController'),  
          startAgentService: jest.fn().mockRejectedValue(new Error('Simulated other error')),
        }));
        process.env.RISE_SA_USERNAME="RISE";
        // Act
        await agentController.startAgent(req, res);

        // Assert
        // expect(res.status).toHaveBeenCalledWith(responseCodes.SERVER_ERROR);
        // expect(res.json).toHaveBeenCalledWith({ flag: 'error', error: 'Simulated other error' });
      });
    });


});
