apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aiops.fullname" . }}-test-connection"
  labels:
    {{- include "aiops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aiops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
