/**
 * Importing Module dependencies for jest test files check jest.config setupFiles key for more information.
 */
require("regenerator-runtime/runtime");
require("dotenv").config();
require('jest-fetch-mock').enableMocks();
